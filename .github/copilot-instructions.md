# Workspace Notes

- This is a Vite + React + TypeScript app.
- Keep the listening-room experience responsive and focused on station selection and playback.
- Run `npm run build` and `npm run lint` after changes.
